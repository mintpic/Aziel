import styles from "@styles/Footer.module.css";

const Footer = () => {
  return (
    <footer className={`${styles.footer}`}>
      Made with ❤️ MintPic project Aziel
    </footer>
  );
};

export default Footer;
